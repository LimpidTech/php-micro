<?php

class AutoLoaderTest extends PHPUnit_Framework_TestCase
{
	/**
	 * A placeholder test which exists because PHPUnit is lame and
	 * therefore:
	 *
	 *    A) doesn't let me have an empty test 
	 *    B) Acts like I didn't specifiy a directory when no tests exist
	 */
	public function testPlaceHolder()
	{
		$this->assertEquals(0, 0);
	}
}

